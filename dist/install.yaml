apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
    control-plane: controller-manager
  name: vaultweaver-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vaultkubernetesrolebindings.security.platform.io
spec:
  group: security.platform.io
  names:
    kind: VaultKubernetesRoleBinding
    listKind: VaultKubernetesRoleBindingList
    plural: vaultkubernetesrolebindings
    singular: vaultkubernetesrolebinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultKubernetesRoleBinding is the Schema for the vaultkubernetesrolebindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VaultKubernetesRoleBinding
            properties:
              audience:
                description: audience is the expected audience for the ServiceAccount
                  JWT.
                type: string
              authMount:
                default: kubernetes
                description: authMount is the Vault auth mount point (e.g., "kubernetes").
                type: string
              boundNamespaces:
                description: boundNamespaces is a list of Kubernetes namespaces that
                  are allowed to login.
                items:
                  type: string
                minItems: 1
                type: array
              boundServiceAccounts:
                description: boundServiceAccounts is a list of Kubernetes ServiceAccount
                  names that are allowed to login.
                items:
                  type: string
                minItems: 1
                type: array
              roleName:
                description: roleName is the name of the Vault role to create/update.
                type: string
              tokenPolicies:
                description: tokenPolicies is a list of Vault policies to assign to
                  tokens issued under this role.
                items:
                  type: string
                minItems: 1
                type: array
              tokenTTL:
                description: tokenTTL is the TTL of the Vault token.
                type: string
            required:
            - authMount
            - boundNamespaces
            - boundServiceAccounts
            - roleName
            - tokenPolicies
            type: object
          status:
            description: status defines the observed state of VaultKubernetesRoleBinding
            properties:
              conditions:
                description: conditions represent the current state of the VaultKubernetesRoleBinding
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last reconciled generation
                  of the resource.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vaultpolicies.security.platform.io
spec:
  group: security.platform.io
  names:
    kind: VaultPolicy
    listKind: VaultPolicyList
    plural: vaultpolicies
    singular: vaultpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultPolicy is the Schema for the vaultpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VaultPolicy
            properties:
              policy:
                description: policy is the HCL policy string.
                type: string
              vaultPolicyName:
                description: vaultPolicyName is the name of the policy in Vault.
                type: string
            required:
            - policy
            - vaultPolicyName
            type: object
          status:
            description: status defines the observed state of VaultPolicy
            properties:
              conditions:
                description: conditions represent the current state of the VaultPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last reconciled generation
                  of the resource.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-controller-manager
  namespace: vaultweaver-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-leader-election-role
  namespace: vaultweaver-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: vaultweaver-manager-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings
  - vaultpolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings/finalizers
  - vaultpolicies/finalizers
  verbs:
  - update
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings/status
  - vaultpolicies/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: vaultweaver-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: vaultweaver-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultkubernetesrolebinding-admin-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings
  verbs:
  - '*'
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultkubernetesrolebinding-editor-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultkubernetesrolebinding-viewer-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - security.platform.io
  resources:
  - vaultkubernetesrolebindings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultpolicy-admin-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies
  verbs:
  - '*'
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultpolicy-editor-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-security-vaultpolicy-viewer-role
rules:
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - security.platform.io
  resources:
  - vaultpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-leader-election-rolebinding
  namespace: vaultweaver-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: vaultweaver-leader-election-role
subjects:
- kind: ServiceAccount
  name: vaultweaver-controller-manager
  namespace: vaultweaver-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
  name: vaultweaver-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: vaultweaver-manager-role
subjects:
- kind: ServiceAccount
  name: vaultweaver-controller-manager
  namespace: vaultweaver-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: vaultweaver-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: vaultweaver-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: vaultweaver-controller-manager
  namespace: vaultweaver-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
    control-plane: controller-manager
  name: vaultweaver-controller-manager-metrics-service
  namespace: vaultweaver-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: vaultweaver
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vaultweaver
    control-plane: controller-manager
  name: vaultweaver-controller-manager
  namespace: vaultweaver-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vaultweaver
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: vaultweaver
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: controller:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: vaultweaver-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
